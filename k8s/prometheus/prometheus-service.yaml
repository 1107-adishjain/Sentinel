apiVersion: v1
kind: Service
metadata:
  name: prometheus-svc
  namespace: sentinel
  labels:
    app: prometheus
    appGroup: sentinel
spec:
  type: NodePort
  selector:
    app: prometheus
    appGroup: sentinel
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 32100
