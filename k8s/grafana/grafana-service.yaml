apiVersion: v1
kind: Service
metadata:
  name: grafana-svc
  namespace: sentinel
  labels:
    app: grafana
    appGroup: sentinel
spec:
  type: NodePort
  selector:
    app: grafana
    appGroup: sentinel
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000
